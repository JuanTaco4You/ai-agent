{
  "name": "@ai-agent/infra",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "typecheck": "tsc -b",
    "lint": "npm run typecheck",
    "test": "echo \"(infra) no tests yet\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "zod": "^3.23.8"
  }
}
